# 图片压缩方案研究计划

## 目标
通过小规模样本测试，确定最优压缩格式和参数组合，为 TB 级 NAS 图片压缩项目提供数据支撑。

## 1. 样本准备

从 NAS 中抽取代表性样本，分为以下几类：

| 类别 | 数量 | 说明 |
|------|------|------|
| 照片-JPEG | 50 张 | 不同场景：风景、人像、夜景、室内 |
| 照片-PNG | 20 张 | 截图、相机导出的高分辨率 PNG |
| 漫画-PNG | 50 页 | 不同风格：黑白日漫、彩色美漫、条漫 |
| 漫画-JPEG | 20 页 | 低质量/高质量各半 |
| CBZ 漫画 | 5 本 | 不同大小的完整 CBZ 文件 |

## 2. 待测格式与参数

### 格式
- **WebP** — 有损（q=75, 80, 85, 90）+ 无损
- **JPEG XL** — 有损（q=75, 80, 85, 90）+ 无损
- **AVIF** — 有损（q=30, 40, 50, 60）+ 无损
- **JPEG** — 仅对现有 JPEG 做质量调整（q=75, 80, 85）
- **PNG** — 仅对现有 PNG 做优化压缩（optipng / pngquant 有损）

### 对照组
- 原始文件（基线）

## 3. 评估指标

| 指标 | 测量方式 | 权重 |
|------|---------|------|
| 压缩率 | 输出大小 / 原始大小 | 高 |
| 压缩耗时 | 单张处理时间（秒） | 高 |
| 解码耗时 | 打开/渲染时间 | 低 |
| 视觉质量 | SSIM / SSIMULACRA2 + 主观评分 | 高 |
| 格式兼容性 | 主流查看器/浏览器支持度 | 中 |
| 解压还原 | 是否可逆（无损格式） | 低 |

## 4. 测试流程

```
1. 收集样本 → 分类存放
2. 对每个样本 × 每个参数组合执行压缩
3. 记录：原始大小、输出大小、压缩耗时
4. 计算 SSIMULACRA2 质量分数
5. 人工抽检：每组合抽 3-5 张目视对比
6. 汇总表格，按类别推荐最优参数
```

## 5. 测试工具

- **压缩**: Pillow (WebP/PNG/JPEG)、pyvips (通用)、cjxl/djxl (JPEG XL)、avifenc (AVIF)
- **质量评估**: SSIMULACRA2（首选）、SSIM（备选）
- **CBZ 处理**: zipfile (解包/打包)
- **计时**: time.perf_counter()

## 6. 预期产出

- [ ] 每个类别（照片/漫画 × 格式）的推荐压缩方案
- [ ] 压缩率 vs 质量曲线图
- [ ] 压缩速度基准数据
- [ ] 最终压缩管线设计文档

## 7. 运行环境

### NAS（持续运行）

| 项目 | 配置 |
|------|------|
| CPU | Intel Xeon E3-1225 v6, 4 核 @ 3.70 GHz |
| GPU | NVIDIA Tesla P4（可用于硬件加速编码）|
| RAM | 16 GB |
| 存储 | /mnt/data1 (12.73 TiB) + /mnt/data2 (12.73 TiB)，mergerfs 合并挂载 |
| OS | NixOS 26.05 (x86_64) |

### 开发机（批量处理存量）

| 项目 | 配置 |
|------|------|
| CPU | Intel Core i5-14600KF, 14 核 @ 5.30 GHz |
| GPU | NVIDIA GeForce RTX 2080 |
| RAM | 32 GB |
| 存储 | 1TB NVMe SSD + NFS 挂载 NAS |
| OS | CachyOS (Arch-based, x86_64) |

### 环境备注
- 开发机通过 NFS 挂载 NAS 目录，直接读写
- 开发机 CPU 更强，用于批量处理存量图片
- NAS 用于后续持续处理新增图片
- btrfs 文件系统，支持快照可用于回滚

## 8. 决策记录

| 决策 | 选择 | 备注 |
|------|------|------|
| 语言 | Python | |
| 格式测试 | 待定 | 需要研究测试 |
| 运行环境 | 本机 | Xeon 4核 + Tesla P4 + 30TB mergerfs |
| 备份策略 | C+B | 原文件移到 .original/，压缩文件移回原位 |
| CBZ 打包 | ZIP (STORED) | 内部图片已压缩，打包时不压缩 |
| 状态追踪 | SQLite | |
| WebUI | FastAPI + 模板 | 后续阶段 |
| 配置格式 | TOML | |
| 新文件发现 | 延后 | 初期专注压缩已有文件 |

## 9. 待决问题

- [x] 备份策略 → C+B 模式
- [x] CBZ 格式 → ZIP (STORED)
- [x] 增量压缩 → 延后，初期专注已有文件
- [x] mergerfs 挂载点路径 → ~/storage
- [x] 扫描哪些目录 → ~/storage/Temp (漫画), ~/storage/本地资源库/福利姬 (照片), ~/storage/Hentai (CBZ)

## 10. 已完成

- [x] 项目结构创建
- [x] CLI 框架 (typer + rich)
- [x] SQLite 状态管理
- [x] 文件扫描器
- [x] 压缩核心 (pyvips + Pillow + CLI 工具)
- [x] CBZ 处理
- [x] 进度显示
- [x] 示例配置
- [x] 样本抽取脚本
- [x] 基准测试脚本 (支持 WebP, JPEG, PNG, JXL, AVIF)
